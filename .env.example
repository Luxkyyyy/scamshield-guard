# Server-only secret used by the Netlify Function on Netlify.
# Get a free key from Google AI Studio. Never prefix it with VITE_.
GEMINI_API_KEY=

# Optional: only needed when running through Lovable AI Gateway.
LOVABLE_API_KEY=